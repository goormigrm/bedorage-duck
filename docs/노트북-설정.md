# 노트북(BOOK-K87ADP9N6K) 설정 — 수동으로 할 것

> 2026-09-04. Claude 가 winget 으로 설치한 것과, 사용자가 직접 해야 하는 것.
> 끝낸 항목은 체크하고 커밋해 두면 다음 세션이 상태를 안다.

## Claude 가 이미 한 것 (2026-09-04)

- [x] Node.js 24.19.0 LTS — `winget install OpenJS.NodeJS.LTS` (C:\Program Files\nodejs)
- [x] GitHub CLI 2.100.0 — `winget install GitHub.cli`
- [x] 저장소 커밋 작성자 — `git config user.name goormigrm` · `git config user.email 1117tkdrms@gmail.com` (bedorage-duck 안에서)
- [x] `npm ci` — node_modules 설치 완료
- [x] git 푸시 자격증명 — Windows 자격증명 관리자에 `git:https://github.com` 이 이미 있어서 `git push` 는 별도 로그인 없이 된다

## 사용자가 직접 할 것

- [ ] **gh CLI 로그인** — 브라우저 인증이 필요해 Claude 가 대신 못 한다. PowerShell 이나 터미널을 새로 열고:

  ```
  gh auth login
  ```

  GitHub.com → HTTPS → "Login with a web browser" → 브라우저에서 코드 입력. 끝나면 `gh auth status` 에 goormigrm 이 보이면 된다.
  없어도 커밋·푸시는 되고, `gh run list`(배포 상태 확인) 만 못 쓴다. Claude 는 그동안 공개 API(curl)로 배포 상태를 본다.

- [ ] **Claude Code 재시작 (한 번)** — 지금 세션은 Node 설치 전에 열려서 PATH 에 node 가 없다. Claude 는 명령마다 PATH 를 직접 붙여서 쓰고 있어 작업엔 지장 없지만, 브라우저 자동 실행(`.claude/launch.json` 의 `npm run dev`) 이 PATH 를 못 찾으면 실패할 수 있다. 이번 세션이 끝나면 Claude Code 를 닫았다 다시 열면 해결된다.

## 확인 명령 (새 터미널에서)

```
node --version     # v24.19.0
npm --version      # 11.17.0
gh --version       # 2.100.0
gh auth status     # 로그인 후 goormigrm
```

## 이 노트북의 작업 폴더

`C:\Users\tkdrm\OneDrive\바탕 화면\철FPS\bedorage-duck` — 메인 PC 와 달리 저장소가 `bedorage-duck` 하위 폴더에 있다. OneDrive 안이므로 커밋 직후 푸시 습관은 동일.
