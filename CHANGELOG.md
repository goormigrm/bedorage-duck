# CHANGELOG

새것이 위. 날짜 · 커밋 단위. 인수인계 요약은 [HANDOVER.md](HANDOVER.md).

## v1.5 — 2026-09-06 (자동 재접속 제거 · 혼자 30초 대기 · 목록 새로고침)

### 자동 재접속을 걷어냈다
실제로 잘 되지 않았다(사용자 확인). **난입이 그 자리를 대신한다** — 끊기면 그냥 나간 것이고, 돌아오고 싶으면 방 목록에서 난입한다.

- 나가면 **3초 뒤 자리를 비운다**(전에는 45초 잡아 뒀다). 자리가 비어야 남들이 난입할 수 있다.
- 브라우저에 자리 표식을 저장하던 것(`bd.rejoin`), 로비의 자동 복귀 시도, `rejoinAsk`/`rejoinAt` 메시지를 지웠다.
- 난입 경로(`joinAsk`/`joinAt`/`resume`)는 그대로다. 코드가 절반으로 줄었다.

### 혼자 남아도 30초 기다린다
전에는 상대가 나가는 즉시 "상대가 모두 나갔습니다" 로 끝났다. 이제 **30초 동안 방을 유지**한다.

- 화면에 `혼자 남았습니다 · N초 안에 아무도 안 들어오면 방이 닫힙니다` 가 카운트다운으로 뜬다.
- 그동안 방은 계속 방송되므로 목록에 "게임 중 · 난입 가능" 으로 보인다. 누가 들어오면 그대로 이어서 한다.
- 30초가 지나면 방을 닫는다.
- 실제 검증: 2인 대전 중 한쪽 탭을 닫자 카운트다운이 뜨고 게임은 계속 돌았다.

### 방 목록 새로고침
공용 로비는 **서로 연결된 사람의 방송만** 보이므로, 늦게 들어왔거나 릴레이가 흔들리면 남의 방이 안 보일 수 있다(사용자 제보).

- 방 목록에 **새로고침** 버튼을 넣었다. 통로를 닫았다 다시 열어 처음부터 다시 찾는다("찾는 중…" 표시).
- 새로고침을 반복해도 타이머가 쌓이지 않게 정리했다.

### 정원이 게스트에게 전달되지 않던 버그
호스트가 정원을 2로 정해도 **게스트 화면에는 4로** 보였다. 방 상태 방송(`room`)에 정원이 빠져 있어 게스트가 제 기본값을 쓰고 있었다. 방송에 넣어 고쳤다.



### 진행 중인 방에 난입
방을 만들 때 **정원을 정하고**, 둘만 모여도 시작할 수 있다. 남은 자리는 **게임 중에 채워진다.**

- 시작할 때 자리를 정원만큼 잡아 둔다. 아직 아무도 없는 자리는 `left` 로 시작해 판에 나오지 않는다.
  (배열 크기를 중간에 늘리면 모두가 정확히 같은 틱에 같은 방식으로 늘려야 해서 어긋나기 쉽다)
- 방 목록에서 게임 중인 방도 자리가 있으면 **난입**을 누를 수 있다. 호스트가 빈 자리를 배정하고,
  **앞선 틱 T** 를 정해 모두에게 알린 뒤 T 에 그 자리를 채운다. 난입자에게는 T 시점의 판 전체를 보낸다(재접속과 같은 길).
- **게임 중에도 방을 계속 알린다.** 전에는 시작하는 순간 방이 목록에서 사라져 아무도 들어올 수 없었다.
- 실제 검증: 두 명이 하는 방에 세 번째 탭이 난입 → 양쪽 tick 1220/1221, 빈 자리가 채워진 것을 확인.

### 최대 인원은 4로 유지 (6·8인 검토 결과)
계산과 대역폭은 8인도 여유였다. 문제는 **풀 메시 연결 수**와 **락스텝의 지터 전파**다.

| 인원 | 시뮬(틱당) | 대역폭 | WebRTC 연결 | 한 명이 1틱 놓칠 때 전원 멈춤 |
|---|---|---|---|---|
| 4인 | 0.026ms | 9.3 KB/s | 6개 | 3.0% |
| 6인 | 0.035ms | 15.5 KB/s | 15개 | 4.9% |
| 8인 | 0.030ms | 21.7 KB/s | 28개 | 6.8% |

8인은 28쌍이 **전부** 뚫려야 하는데 TURN 없이 STUN 만 쓰므로 한 쌍만 실패해도 그 둘은 게임이 안 된다.
사용자가 3인(한 명 무선)으로 실제 테스트했을 때 이미 끊김이 체감됐다 → **4인으로 유지.**

### 끊김(순간이동) 완화
증상: 상대 입력이 늦어 잠깐 멈췄다가 밀린 틱을 한꺼번에 처리하면서 캐릭터가 **순간이동**처럼 보인다.

- **입력 지연 하한을 3틱(50ms)** 으로 올렸다(전 2틱). RTT 가 낮아도 지터가 있으면 한 틱만 늦어도 전원이 멈춘다.
- **한 번에 따라잡는 틱을 8 → 4** 로 줄였다. 나머지는 다음 호출에서 처리한다.
- **화면 위치를 부드럽게 따라가게** 했다(`dispPos`). sim 위치로 즉시 점프하지 않고 상한 속도로 접근한다.
  리스폰·난입처럼 2타일 넘게 벌어지면 즉시 붙인다.
- 이것으로 체감은 줄지만 **없어지지는 않는다.** 락스텝은 가장 느린 사람에 맞춰 도는 구조이고,
  서버 없이(무료) 하는 이상 근본 해결은 롤백 넷코드뿐이다. 그건 별도 큰 작업이라 하지 않았다.

### 로비 재배치 · 닉네임
- **2열 배치**: PC 는 왼쪽에 닉네임·내 방·방 목록·방 만들기, 오른쪽에 캐릭터·맵·혼자 하기·최근 전적.
  모바일·좁은 화면에서는 그 왼쪽 열이 **맨 위**로 온다. 방 목록이 최근 전적에 밀려 아래로 가던 것을 고쳤다.
- **닉네임 입력을 맨 위로** 옮기고 크게 만들었다. 안 정했으면 카드가 노랗게 강조된다.
  전에는 참가를 누르고 나서야 "닉네임을 먼저" 라는 안내를 봤다.

## v1.3.1 — 2026-09-06 (팀전 관전 · 리스폰 자리 노출)

사용자 제보 3건.

1. **리스폰하는 순간 적의 새 자리가 미니맵에 잠깐 보였다.**
   죽어 있는 동안에도 "0.22초 전까지 보였다"는 기억이 남아 있어서, 살아나는 순간 그 기억으로 새 위치가 그려졌다.
   → **죽어 있는 사람은 기억을 지우고 무조건 숨긴다.** 살아나면 그 자리에서 새로 판정한다. 개인전도 마찬가지다.
2. **팀전에서 죽으면 상대 시점이 보였다.** 적 위치가 그대로 드러나 팀전이 성립하지 않는다.
   → **팀전에서는 아군만** 본다. 볼 아군이 없으면 내 자리에서 본다. 개인전은 그대로 누구든 볼 수 있다(곧 리스폰하고, 배우는 재미가 있다).
3. 관전 안내를 **`◀ A · D ▶ 로 바꾸기`** 로 바꿨다(좌우 화살표도 그대로 된다). 볼 사람이 하나뿐이면 "바꾸기" 안내를 빼고 이름만 보여 준다.

- `tests/spectate.test.ts` 5개 추가(팀전 아군 제한·개인전 자유·나간 사람 제외·죽은 사람 숨김·리스폰 위치 변화). 총 79개.

## v1.3 — 2026-09-06 (끊겨도 다시 들어오기)

무선이나 모바일에서는 실제로 자주 끊긴다. 그때마다 판이 끝나 버리면 4인 대전이 성립하지 않는다.

**자리를 45초 비워 둔다.** 누가 끊기면 바로 지우지 않고 자리를 잡아 둔다.
그동안 락스텝은 그 사람 입력을 빈 입력으로 채우므로 **남은 사람들의 경기는 멈추지 않는다**(캐릭터는 그 자리에 서 있다).
45초가 지나면 그때 자리를 없앤다.

**돌아오면 그 지점부터 이어서 한다.**
1. 판이 시작될 때 각자 자기 자리 표식(방 코드 + 자리 번호)을 브라우저에 저장한다.
2. 끊긴 사람이 게임을 다시 열면 **로비가 먼저 그 방으로 돌아가 본다**(2분 안).
3. 호스트가 표식을 확인하고 **앞선 틱 T**(2초 뒤)를 정해 모두에게 알린다. 모두 그 틱부터 그 사람 입력을 다시 기다린다.
4. 호스트가 T 에 도달하면 **그 시점의 판 전체**를 돌아온 사람에게 보낸다. 받은 쪽은 그것으로 갈아 끼우고 이어서 돌린다.
   맵의 모래주머니 상태도 함께 맞춘다(`syncSandbags`).
5. 자리가 이미 사라졌으면 호스트가 거절하고 평범한 로비로 돌아간다.

- `Lockstep` 에 `rejoin(idx, fromTick)` 과 시작 틱을 넣었다. 지나간 틱은 빈 입력으로 메워 **이미 지나간 지점에서 멈추지 않게** 한다.
- 실제 검증: 브라우저 두 탭으로 대전 중 한쪽을 새로고침(=끊김) → 자동 복귀 → **양쪽 tick 3490, 체력·위치까지 완전히 같음**.
- `tests/rejoin.test.ts` 5개 추가(빈 입력 채우기·다시 기다리기·도중 합류·판 이어받기 해시 일치·모래주머니 복원). 총 74개.

## v1.2 — 2026-09-06 (관전 · 피격 방향 · 결과 통계 · 팀 신호)

검토 목록에서 사용자가 고른 네 가지. 리플레이는 "가볍게 즐기는 게임이라 다시 볼 이유가 없다" 며 뺐다.

### 죽었을 때 상대 시점 보기
- 죽으면 **나를 죽인 사람 시점**으로 카메라와 시야가 옮겨 간다. 리스폰하면 자동으로 돌아온다.
- `←`/`→`(또는 `A`/`D`)로 다른 사람으로 바꿀 수 있다. 화면 아래에 누구 시점인지 뜬다.
- 렌더 옵션에 `viewer` 를 두어 **카메라·시야만** 그 사람을 따르게 했다. HUD 의 내 카드는 그대로 나를 가리킨다.

### 피격 방향 표시
- 맞으면 화면 가장자리에 **쏜 쪽을 가리키는 호**가 뜬다(큰 피해는 굵고 붉게). 시야가 좁은 게임이라 어디서 맞았는지 모르면 대응할 수가 없었다.
- 각도는 **화면 기준**(`worldDirToScreen`)이라 호가 가리키는 쪽을 그대로 보면 된다.
- 비슷한 방향이면 새로 만들지 않고 수명만 되살려, 연사에 화면이 지저분해지지 않게 했다.

### 결과 화면 상세 통계
- 킬·데스에 더해 **연속 킬·명중률·헤드샷 비율·준 피해·받은 피해**를 표로 보여 준다. 내 줄은 강조된다.
- 수치는 전부 **sim 안에서** 센다(`PlayerState.shots/hits/heads/dmgDealt/dmgTaken/bestStreak`). 그래야 리싱크·재접속에도 값이 어긋나지 않고 모두의 화면이 같다.
- 명중률은 **탄 단위**다. 산탄총 한 발은 탄 7개라 방아쇠 횟수로 세면 산탄총만 700% 처럼 보인다.
- 막힌 탄(후라이팬)은 피해가 0 이라 명중으로 세지 않는다.

### 팀 신호 (2v2)
- 팀전에서 `V`(모바일은 **신호** 버튼)로 커서가 가리키는 곳에 **"여기"** 를 찍는다. 같은 편에게만 보이고 6초 뒤 사라진다.
- 컨트롤 메시지(`mark`)로 보낸다 — **sim 밖이라 결정론과 무관**하다. 봇전에서는 내 화면에만 남는다.
- 총소리와 헷갈리지 않게 짧고 맑은 두 음으로 알린다.

- `tests/stats.test.ts` 6개 추가(명중·헤드샷·탄 단위 발사 수·연속 킬·막힌 탄 제외·스냅샷·봇 경기 정합성). 총 69개.

## v1.1 — 2026-09-06 (모바일 메뉴 · 맵 통행 · 힐팩)

### 문서 정리 (v1.1 기준으로 통일)
- 버전 표기를 맞췄다: `package.json` 1.1.0, DESIGN 헤더·기준일, README, PREP, 공지글.
- 플레이 가이드에 **힐팩 요령**과 **소리로 방향 알기**를 넣고, 후라이팬 설명을 "절반쯤 막는다"로 고쳤다(50% 방어 반영). 모바일 소리 끄기 위치도 ≡ 메뉴로.
- 공지글에 힐팩·방향 소리 두 줄을 넣고 승빠덕 설명을 고쳤다.
- HANDOVER "다음 할 일"을 다시 썼다: 사용자가 해야 하는 것 / 피드백이 오면 손볼 것 / **검토 대기(제안 목록)** 로 나눴다.


### 모바일 메뉴를 ≡ 하나로
- ≡ 버튼을 **왼쪽 위 → 오른쪽 위**로 옮기고, 위에 있던 '소리 켜짐'·'로비로'를 **≡ 메뉴 안**으로 넣었다.
  화면 위쪽에는 이제 가운데 점수판과 오른쪽 ≡ 만 남는다.
- 메뉴에 소리 토글이 생겼다(라벨이 상태에 따라 '소리 끄기'/'소리 켜기'). 데스크톱은 기존 상단 버튼을 그대로 쓴다.
- 숨김 기준을 미디어 쿼리가 아니라 `.game-root.touching` 클래스로 바꿔, `?touch=1` 로 PC 에서 시험할 때도 폰과 같게 보인다.

### 맵: 갇히는 곳 없이, 뺑 돌지 않게
사용자 지적: "스튜디오가 미로 같아서 뺑 돌아가야 하는 경우가 많다".
기존 `ensureConnected` 는 **갇힌 곳만** 없앨 뿐, 길이 이어져 있어도 한참 돌아가는 구조는 그대로 뒀다.

- `openShortcuts` 추가 — 걸음 수 거리를 재서 **바로 옆인데 멀리 돌아가야 하는 벽**에 구멍을 낸다(최대 3칸 두께까지 관통, 2칸 폭).
- `straightenPaths` 추가 — 그래도 남는 구간은 맵을 격자로 훑어 **직선 거리 대비 걷는 거리(우회도)** 가 가장 나쁜 쌍을 찾아 직선으로 잇는다.
- 방을 덜 잘게 쪼개고(최소 변 6→8) 문을 벽 길이에 비례해 더 냈다(1~2개 → 2개 + 길이/14).
- 결과 (실제 8방향 이동 기준, 1.0 이면 곧게 통함):

| 맵 | 평균 | 최악 (고치기 전) |
|---|---|---|
| 스튜디오 x1 | 1.09 | **1.42** (1.86) |
| 스튜디오 x2 | 1.08 | **1.36** (1.68) |
| 마당 | 1.06~1.07 | 1.13~1.23 |
| 주차장 | 1.07~1.09 | 1.18~1.36 |

전 맵·전 배율에서 **못 가는 곳 0**. 맵 생성 시간은 x1 7ms · x4 102ms (판 시작 때 한 번).

### 죽은 자리에 힐팩
사용자 요청: "상대가 죽으면 그 자리에 힐팩을 떨어뜨려 이긴 사람이 이점을 갖게".

- 죽으면 그 자리에 힐팩이 떨어진다. 최대 체력의 **35%** 회복, **20초** 뒤 사라진다(사라지기 3초 전부터 깜빡인다).
- 다친 사람만 줍는다. 체력이 가득이면 밟아도 남는다. 동시에 밟으면 번호가 앞선 사람이 줍는다(양쪽 화면에서 같아야 하므로).
- 한 번에 6개까지만 남는다(많으면 지저분하고 너무 유리해진다).
- 봇도 체력이 65% 아래면 가까운 힐팩을 주우러 간다. 안 그러면 사람만 이득이라 대전이 기운다.
- 흰 상자에 빨간 십자로 표시하고, 떨어질 때와 주울 때 소리·이펙트가 난다.
- `tests/medkit.test.ts` 8개 추가(드롭·회복·상한·거리·소멸·스냅샷/해시·봇 결정론). 총 63개.

## v1.0.3 — 2026-09-06 (밸런스 표에서 승빠덕 제외 · 모바일 화면 · 발소리)

### 밸런스 표에서 승빠덕을 뺀다
사용자 지적: "승빠덕은 근접이라 사람이 조절하면 사기급인데 자동 밸런스 테스트에서는 나쁘게 나온다".
맞다. 봇은 "굴러서 붙고 막으며 버티는" 운용을 못 해서 표가 실제와 반대로 나오고, 승빠덕과 붙는 상대의 승률까지 부풀린다.

- `CharacterDef.skipBotBalance` 추가(승빠덕만 true) → `BOT_BALANCE_LIST`. `tools/balance.ts`·`tests/balance.test.ts` 가 이 목록을 쓴다.
- 표에서 빼니 나머지 11명 승률이 정직해졌다: **41.3 ~ 58.8%** (승빠덕 포함 때는 38.6~61.4%).
- 승빠덕은 `tools/melee.ts` 로만 본다.

### 모바일 화면 (사용자 제보 2건)
1. **점수판과 '소리 켜짐'·'로비로' 버튼이 겹쳤다.** 버튼이 게임 화면(stage) 안에 있어 축소되면서 가운데 점수판과 부딪혔다.
   → 터치 기기에서는 버튼을 **실제 화면 우상단에 고정**하고 손가락 크기로 키웠다(노치 안전영역 반영).
2. **왼쪽에 검은 여백이 크고 화면이 오른쪽으로 붙었다.** 원인 두 가지:
   - 1280×720 고정 화면을 폰 가로(약 2.2:1)에 넣으니 좌우가 남았다.
     → **논리 폭을 화면 비율에 맞춰 늘린다**(높이 720 고정, 폭 1280~1728). 폰에서 여백이 사라진다.
     넓어진 만큼 좌우로 더 보이면 넓은 화면이 유리하므로, **세로 시야를 좁혀 보이는 면적을 일정하게** 맞췄다(`renderer3d.resize`).
   - 화면보다 큰 요소를 flex 로 가운데 두면 한쪽으로 쏠린다.
     → `left:50%/top:50%` + `translate(-50%,-50%)` 로 정확히 가운데. 960×400 에서 좌우 여백 58px 로 **대칭** 확인.

### 발소리 · 재장전 · 방향감
- **발소리 추가.** sim 이벤트로 만들면 틱마다 생겨 패킷·해시가 무거워지므로, 이동 상태를 보고 소리 쪽에서 걸음 위상을 굴린다(`Sfx.updateSteps`). 캐릭터 속도·정조준·다리 부상에 따라 보폭이 달라지고, 좌우 발을 번갈아 조금 다른 음으로 낸다. 내 발소리는 가운데에서 작게, 남의 발소리는 방향·거리대로. 구르는 중에는 내지 않는다.
- **재장전 소리를 세 박자로** 다시 만들었다(탄창 빼기 → 끼우기 → 노리쇠). 전에는 딸깍 두 번이라 재장전으로 들리지 않았다.
- **방향감을 화면 기준으로 고쳤다.** 카메라가 45° 돌아가 있는데 패닝을 월드 x축으로 하고 있어서, 들리는 방향과 보이는 방향이 어긋났다. 이제 `worldDirToScreen` 으로 돌린 뒤 좌우 성분으로 패닝한다.
- 거리 표현도 손봤다: 감쇠를 역제곱에 가깝게 바꾸고, **멀수록 고음을 깎는다**(공기 흡수 흉내). 아주 가까우면 가운데로 모아 귀에 붙는 느낌을 없앴다.

## v1.0.2 — 2026-09-06 (후라이팬 방어 50% 확률)

사용자 지시: "승빠덕 지금도 너무 세니까 후라이팬으로 막는 걸 50% 확률로".

- `BLOCK_CHANCE = 0.5` 추가. 앞쪽(±75°)에서 오는 탄이라도 **절반만** 막고 나머지는 그대로 맞는다.
  추첨은 `rand(state.rng)` — sim 의 시드 난수라 두 브라우저에서 결과가 같다(결정론 유지).
- 효과: 기력이 바닥나기 전에 체력이 먼저 닳는다. 정면 대치(`tools/melee.ts`, 구르지 않고 버티기) **2/10 → 0/10**.
- 봇 1:1 528판 승률: 승빠덕 **42.0% → 38.6%**, 전체 밴드 42.0~56.8% → **38.6~61.4%**.
  승빠덕이 약해지면서 그와 붙는 캐릭터들이 이득을 봤다(주우재덕 45%대 → 61.4%).
  밴드를 다시 좁히려면 승빠덕 체력·이동 속도나 후라이팬 피해를 올려야 한다. 지시 범위 밖이라 손대지 않았다.
- `tests/block.test.ts`: "계속 쏘면 뚫린다" 를 새 규칙(체력이 먼저 닳아 죽는다)에 맞게 고치고, 막는 비율이 35~65% 안에 드는지 보는 통계 테스트를 더했다. 총 54개 통과.
- 문서: DESIGN 3.1·캐릭터 표, HANDOVER 규칙 13, 승빠덕 패시브 설명, `tools/melee.ts` 기준 문구.

## v1.0.1 — 2026-09-05 (후라이팬 방어 · 밸런스 재조정)

사용자 제보: "승빠덕이 안 구르고 그냥 막고 때리면 거의 무적이다".

**재현**: `tools/melee.ts` 를 새로 만들어 승빠덕이 구르지 않고 정면으로 버티는 대치를 10판 돌렸다.
고치기 전에는 **10판 모두 15초 동안 살아남았고**(체력 87~245 남음) 총알을 계속 튕겨 냈다. 제보가 맞았다.

**원인**: 맞고 있는 동안에도 기력이 계속 찼다. 기력 회복이 초당 39.6, 막는 비용이 피해 1당 0.55 →
**초당 72 피해를 영구히 상쇄**한다. 소총 DPS 가 138 이니 절반 이상을 무한히 튕겨 내는 셈이었다.

**고친 것**: 막은 직후 0.5초는 기력이 차지 않는다(`BLOCK_LOCK_TICKS`). 이제 기력 한 통(피해 181)까지만 막고
계속 쏘면 뚫린다. 대신 안 맞을 때의 회복은 1.8배 → 2.4배로 올려 교전 사이 복구는 빠르게,
체력도 245 → 260 으로 올렸다. 정면 대치 승률 **10/10 생존 → 2/10 승리**.

- 헤드샷은 막지 못하게 하는 안(사용자 제안)도 재 봤지만, 사람이든 봇이든 상대 한가운데를 겨누므로 명중 대부분이 머리 판정이라 방어가 아예 무의미해졌다(승률 18~38%, 정면 대치 0/10). 채택하지 않았다.
- 겸사겸사 **전체 밸런스를 다시 쟀다.** v1.0 의 맵 생성 변경(3종·모래주머니 감소) 이후 표본이 밀려 있었다: 매직덕 68.2% · 주우재덕 60.2% 로 밴드를 벗어나 있었다.
  매직덕 260→235, 주우재덕 200→185·대시 거리 +50%→+30%, 침착덕 200→190, 후라이팬 연사 26→23틱.
  **결과 밴드 42.0 ~ 56.8%** (v1.0 은 42~58%, 재측정 직전은 18~68%).
- `tests/block.test.ts` 6개 추가 — 맞는 동안 기력이 안 차는지, 계속 쏘면 뚫리는지, 뒤에서 오는 탄은 못 막는지.
- `tools/melee.ts` 추가. 봇 표(`tools/balance.ts`)는 "사람이 의도적으로 막기를 유지하는" 플레이를 재현하지 못하므로, 후라이팬은 이 도구로 따로 본다.

## v1.0 — 2026-09-04 (개발 1차 완료)

기능 개발을 여기서 끊는다. 아래는 마지막 정리.

- **모래주머니**: 지오메트리 바닥이 y=0 인데 인스턴스를 다시 h/2 만큼 올리고 있어 **공중에 떠 보이던 것**을 바닥에 붙였다. 자루도 실제 방벽처럼 **3 - 2 - 3 세 단**(가운데 단은 이음매가 어긋나게)으로 다시 쌓았다. 판정·내구도는 그대로.
- **문서 v1.0 정리**: DESIGN 의 잘못 끼어 있던 절 순서(7.1 이 8.3 뒤에 있었다)를 바로잡고, 성능 수치·저장소 구조·테스트 목록을 실제와 맞췄다. HANDOVER 에 **"다른 PC 에서 이어받기"** 절과 사용자 진행 방식·UI 톤·비용 원칙을 옮겨 적어, 새 PC 에서 `git pull` 만으로 이어갈 수 있게 했다. PREP 에 전적 기록 원칙(D)을 추가하고 남은 항목을 갱신했다.
- **모래주머니 수를 절반 이하로.** 중앙 진지를 12칸 → 8칸(모서리를 비워 사방 출입)으로 줄이고, 4배 맵의 진지를 5개 → 2개로, 흩어진 줄도 맵마다 1~2개로 줄였다. 기본 맵 기준 25~39칸 → **12~15칸**. 어디서나 엄폐가 되면 "붙은 쪽이 일방적으로 때린다" 는 규칙 자체가 싱거워진다.
- `tests/sandbag.test.ts` 추가 — 엄폐 규칙을 실제 사격으로 검증한다: 붙어 있으면 헤드샷이 아니어도 넘어가고, 밖에서 쏜 몸통 탄은 막히고, 정확히 머리를 겨눈 탄만 넘어오고, COVER_DIST(46px)를 벗어나면 다시 막힌다.
- 쓰지 않게 된 `screenDirToWorldAngle`(옛 터치 조준 스틱용)을 지웠다. `package.json` 버전 1.0.0.
- 테스트 47개 · `npm run build` 통과.

## 2026-09-04 (8차)

### 프리뷰 페이지 삭제
- `preview.html` · `src/preview.ts` 를 지웠다. 트레일러·시트 촬영용으로 두었던 봇 대 봇 페이지인데, 이제 캐릭터 시트도 스크린샷도 다 뽑았고 유지할 이유가 없어졌다. 배포 산출물이 한 덩어리로 줄었다.
- 스크린샷이 다시 필요하면 실제 게임을 `?shot=1` 로 열어 캔버스를 합쳐 `POST /__shot` 하면 된다(개발 서버 전용).

### 대기실 글씨
- 한 줄로 붙어 있던 방 설정(맵·모드·목표·인원·핑)을 **라벨 붙은 조각들**로 쪼갰다.
- 자리 카드: 높이를 키우고 캐릭터 이름과 준비 상태를 줄바꿈, 빈자리는 큰 제목 글꼴 대신 작고 흐리게.
- 방 코드 12→15px, 상태 줄 13.5→15px(굵게), 링크 12→13px, 준비 버튼도 키웠다. "둘 이상 모두 준비하면 시작" 은 별도 줄로 뺐다.

### 모바일 조작 다시 (실제 폰 사용 후)
- **조준 스틱을 없애고 자동 조준으로.** 폰에서 스틱 두 개를 정확히 미는 것은 무리였다. 이제 왼쪽은 이동, 오른쪽은 **누르고 있으면 사격**(큰 사격 버튼도 같다).
- 자동 조준은 **보이는 적**만 고르고(시야 반경 + 벽 판정), 한 틱에 최대 5.6°(초당 약 340°)만 돌아간다. 즉시 겨누지 않으므로 뒤를 잡히면 도는 시간이 걸린다. 조준각은 평소처럼 `Input` 에 실려 나가 동기화에 영향이 없다.
- **'로비로' 를 비롯한 창 버튼이 안 눌리던 문제**: 터치 조작 루트가 화면 전체를 덮고 있었다. 루트는 `pointer-events: none` 으로 두고 조작 요소만 받도록 고쳤고, 창이 뜨면 조작을 아예 감춘다.
- 화면 위 64px 은 조작 영역에서 빼고(소리·로비로 자리), HUD 의 '다른 사람 목록'은 버튼과 겹치지 않게 미니맵 아래로 옮겼다. 터치 중에는 키보드 안내를 감춘다.
- `tests/autoaim.test.ts` 추가 (표적 선택·회전 속도 제한·벽 뒤 제외·이동 방향 폴백).

### 키가 위에서도 보이도록
- 몸통만 세로로 늘려서는 쿼터뷰에서 키 차이가 안 보였다. 이제 `tall` 이 **다리 길이까지 늘린다**(`1 + (tall-1) × 2.2`). 몸이 그림자에서 멀어지고, 걸음도 다리 길이에 비례해 크게 젓는다(`rig.stride`).
- 키 폭도 넓혔다. 화면상 머리끝 높이 기준 주우재덕 82px ↔ 풍월덕 39px (전에는 61 ↔ 43). 순서는 그대로.
- 주우재덕은 너무 가늘어 작아 보이던 것을 조금 넓혔다(`slim` 0.72 → 0.78).

## 2026-09-04 (7차)

### 모바일 (가로 화면 전용)
- 터치 조작 `src/game/touch.ts`: 화면 왼쪽 절반 = 이동 스틱, 오른쪽 절반 = 조준 스틱(끝까지 밀면 사격). 스틱은 손가락을 대는 자리에 생긴다. 오른쪽 가장자리에 조준 / 구르기 / 재장전 / 교체, 왼쪽 위에 메뉴 버튼.
- 세로로 들면 "가로로 돌려 주세요" 안내가 화면을 덮는다. 시작할 때 전체화면 + `screen.orientation.lock('landscape')` 을 시도한다(iOS 는 잠금을 지원하지 않아 안내로 대체).
- 거친 포인터(`pointer: coarse`)로 판별한다. PC 에서 시험하려면 주소 뒤에 `?touch=1`.

### 맵 생성 3종
- 뼈대 만드는 방식을 맵마다 나눴다: **rooms**(영역을 재귀로 갈라 방과 3칸 문) · **scatter**(벽 덩어리 흩뿌리기) · **pillars**(기둥 격자).
- 세 번째 맵 **주차장**(44×28, 기둥) 추가.
- 마지막에 갇힌 바닥이 없도록 벽을 뚫어 잇는 연결 보정을 넣었다. 시드 10개로 검사한다.

### 전적 기록 (서버·DB 없이)
- 판이 끝나면 **닉네임·캐릭터·킬·데스·팀·중도 퇴장**까지 그 판의 결과를 브라우저(`localStorage`)에 남긴다. 락스텝이 결정적이라 같은 방에 있던 사람들은 자동으로 같은 내용을 갖게 된다. 최근 50판.
- 로비 아래 **"최근 전적"** 카드 목록. 승/패 색, 내 줄 강조, 팀 배지, 나감 표시. **복사** 버튼이 게시판·채팅에 붙여 넣을 글을 만든다(클립보드가 막히면 선택된 텍스트 상자로 대체).
- 전체 순위표는 만들지 않는다 — 남의 기록까지 모으려면 저장소(서버·DB)가 필요하고, 카드 등록이 필요한 서비스는 쓰지 않기로 했다. 근거와 한계는 `docs/DESIGN.md` 8.4.
- `tests/records.test.ts` 추가 (정렬·승자 표시·공유글 형식).

### 저격 조준경 손보기
- 조준경을 켜면 **내 주변에도 구멍이 하나 더** 생긴다. 멀리 보면서도 붙는 상대를 볼 수 있다.
- 조준 방향으로 미는 거리 8→**5**타일, 카메라 거리 1.75→**1.45**배, 조준경일 때 카메라 추종을 절반 속도로. 조준선이 화면을 훑는 속도가 줄어 맞히기 쉬워졌다.

### 키와 몸 폭
- `look.tall`(키) 추가, 선언만 되어 있고 쓰이지 않던 `look.slim`(몸 폭)을 실제로 적용했다. 둘 다 보기용이며 판정(2D 원)에는 영향이 없다.
- 실제 인물 기준 키 순서를 그대로 따른다: 주우재덕 > 기열덕 > 침착덕 > 우원덕 > 매직덕 > (철면덕 = 승빠덕 = 주펄덕 = 옥냥덕) > 통닭덕 > (단군덕 = 풍월덕). 주우재덕은 가장 크고 가장 가늘다.
- **통닭덕 머리**: `hair: 'bob'` 이 3D 쪽에 케이스가 없어 민머리로 나오고 있었다. 정수리 캡 + 볼 옆·뒤 커튼 + 일자 앞머리로 검은 단발을 그린다.

### 그 밖
- 캐릭터 시트를 단체 사진 배치(앞줄 6 · 뒷줄 6, 단 위)로 다시 잡고, 무기·체력·원본을 같이 적는다.
- 개발 서버 전용 스크린샷 저장(`POST /__shot` → `docs/img/`)과 프리뷰 캡처 훅(`?shot=1`). 배포물에는 들어가지 않는다.

## 2026-09-04 (6차)

### 모래주머니 겉모습 · 문서 전면 정리
- **모래주머니**를 그냥 색 다른 네모에서 **자루를 두 줄로 쌓은 더미**로 바꿨다(눌린 구 6개를 합친 지오메트리, 밝은 모래색). 내구도가 닳으면 색이 점점 어두워져 곧 터진다는 것이 보인다. 미니맵 색도 상자·벽과 확실히 구분되게 밝혔다.
- **문서 정리**: `docs/DESIGN.md` 를 v1.0 으로 전면 재작성(초안 본문 폐기, 현재 규칙·수치·구조·결정론 규칙·성능·밸런스 방법론), `docs/플레이-가이드.md` 신설(처음 하는 사람용), `docs/공지글-모음.md` 확정(초안 딱지 제거·12명 반영), `README.md` 재작성, `docs/PREP.md` 남은 항목만으로 정리, `HANDOVER.md` v1.0 상태로 갱신, DECISIONS·노트북 설정에 보관용 표시.

## 2026-09-04 (5차)

### 밸런스 실전 조정 (봇 528판 계측 → 8차례 수정)
- **계측 도구** `tools/balance.ts` 추가: 봇끼리 1:1 전 조합(528판)과 4인 개인전(72판)을 돌려 캐릭터별 승률·K/D·주고받은 피해, 무기별 명중률·헤드샷 비율·거리 구간별 명중을 표로 뽑는다. `npx vite-node tools/balance.ts [ffa]`.
- **처음 측정에서 드러난 것**: 승빠덕 승률 **0%**(후라이팬으로 접근 자체가 불가능, 528판에서 105번밖에 못 휘두름), 풍월덕 22.7%, 철면덕 34.1%, 반대로 소총 캐릭터가 77~78%, 저격총이 64%.
- **최종(1:1 528판)**: 42.0% ~ 58.0% 밴드. 철면덕 58.0 · 옥냥덕 55.7 · 주우재덕 55.7 · 우원덕 53.4 · 통닭덕 52.3 · 단군덕 51.1 · 침착덕 50.0 · 매직덕 48.9 · 승빠덕 44.3 · 주펄덕 43.2 · 기열덕 43.2 · 풍월덕 42.0.
- 무기: 소총 26→23 피해·반동 상향·원거리 감쇠 추가(장거리 지배 완화), 권총 26→27·연사 13→11틱, SMG 15→16·감쇠 시작 260→320px, 산탄총 탄당 14→17·집탄 9°→7.5°·연사 42→38틱, 기관총 14→16·퍼짐 12°→8°·이동 0.86→0.9, 저격총 120→112·연사 90→78틱·이동 0.8→0.7, 후라이팬 62→80·사거리 62→70px.
- 캐릭터: 철면덕 270→290, 단군덕 180→200, 풍월덕 170→220, 통닭덕 240→220, 매직덕 300→260(회복 초당 6), 승빠덕 220→245·대시 쿨다운 90→34틱·이동 3.4→3.7.
- 승빠덕 "방패" 재설계: 막을 때 **피해 1당 기력 0.55** 소모, 기력 회복 1.8배. 봇도 근접 무기면 옆으로 새지 않고 굴러서 파고들도록 수정.
- 기열덕 "뇌절": 빗나가면 초기화 → **한 단계씩 감소**, 명중당 +9%(최대 +54%).
- 헤드샷 범위를 반지름의 34% → **28%** 로 좁혔다 (명중 중 헤드샷 비율 37% → 31%).
- **회귀 테스트** `tests/balance.test.ts`: 1:1 전 조합에서 모든 캐릭터가 20~80% 안에 들고, 모든 무기가 실제로 명중하는지 매번 검사한다.

## 2026-09-04 (4차)

### 사진 기반 2차 멤버 · 승빠덕 개명
- 참고 사진대로 다시: 우원덕(검은 앞머리·남색 정장·패턴 넥타이) · 기열덕(갈색 머리·크게 벌린 입·빨간 넥타이) · 풍월덕(금발 버섯머리·하늘 후드) · **옥냥덕(주황 고양이 아바타 — 고양이 귀·흰 얼굴 무늬·수염·내민 혀·노란 후드)** · 통닭덕(검은 단발·빨강·하늘 줄무늬 스웨터) · **주우재덕(검은 가죽 재킷·크림 셔츠·진청 데님, 아주 마르고 길게 `slim: 0.68`)** · **승우덕 → 승빠덕**(검은 볼캡+빨간 핀·검은 티·파란 앞치마).
- `Look` 확장: `hair: 'bob'`, `mouth: 'shout'`, `animal: 'cat'`, `furColor`, `slim`, `stripe`, `apron`, `hood`, `pants`, `capPin`.

### 전투 규칙 대개편
- **체력 2배** (170~300). 총을 여러 발 주고받아야 죽는다.
- **무기 재배치**: 철면덕 기관총(신규), 매직덕 산탄총, 풍월덕 산탄총, 통닭덕 저격총, **승빠덕 후라이팬(근접)**.
- **무기 밸런스 전면 조정**: 저격총만 한 방(몸 120·머리 240)에 죽일 수 있고, 나머지는 한 번의 사격으로 죽지 않는다(테스트로 강제). 저격총 재장전 260틱(4.3초), 지향사격 퍼짐 15°(정조준 0.4°) → **우클릭 없이는 거의 못 맞힌다**. 기관총은 퍼짐 12°·탄창 80·연사 5틱.
- **후라이팬**: 꾹 누르면 계속 휘두르는 부채꼴 근접 공격(사거리 62px, ±55°, 62 피해). 탄약 무한.
- **기력(스테미나)** 신설: 최대 100, 초당 22 회복. 대시가 34 소모. **근접 무기를 든 사람은 앞에서 오는 총알을 기력으로 막는다**(막은 피해만큼 기력이 닳고, 기력이 떨어지면 그대로 맞는다).
- **대시 = 구르기**: 구르는 동안 무적(이전 커밋)에 더해 기력 소모.
- **헤드샷 조건 변경**: 확률 추첨이 아니라 **탄 궤적이 상대 중심을 얼마나 정확히 지나는가**로 정한다(중심 0.34r 이내 머리 ×2, 0.74r 이내 몸통, 그 밖 다리 ×0.6). 산탄은 머리 배율 1.5.
- **목표 킬 5~50 (5 단위)** 선택.
- **AI 하향**: 예전 '쉬움'이 지금의 '보통' 수준. 반응·조준 정확도·발사 확률·회피를 모두 낮췄다.

### 모래주머니 (덕코프식 엄폐물)
- 맵 가운데(넓은 맵은 사분면에도)에 사방 입구가 있는 진지가 생기고, 곳곳에 짧은 줄이 흩어진다.
- **일방적인 딜교환**: 모래주머니에 붙어 쏘면(46px 이내) 내 탄은 넘어가고, 상대 탄은 막힌다. **정확히 머리를 겨눈 탄은 엄폐와 무관하게 넘어간다**.
- 시야는 넘어간다(낮은 엄폐물). 내구도 240, 부서지면 지나갈 수 있다.

### 맵 랜덤 생성 · 미니맵 · 대기실 미리보기
- `buildMap(id, scale, seed)`: 테두리·크기만 문자 그리드에서 오고 **안쪽 구조물(벽 덩어리·상자·모래주머니)은 매 판 시드로 생성**. 같은 시드면 모든 브라우저가 같은 맵. 스폰도 생성 결과에서 서로 멀리 떨어지게 고른다. 통로가 막히지 않는지 테스트로 검증.
- 왼쪽 위 **미니맵**(벽·상자·모래주머니, 나·아군·보이는 적).
- 로비 맵 선택 옆에 **대략적인 맵 미리보기**(스폰 노란 점).

### 저격 조준경 · 시야 · 표시
- 저격총 우클릭 시 **조준경**: 커서 둘레만 남기고 어두워지고 십자선·눈금이 그려지며, 카메라가 멀어지고 **시야 반경이 1.8배**로 늘어난다.
- 전장의 안개 가장자리를 부드럽게(해상도 상향 + 블러). 계단처럼 보이던 현상 제거.
- **깜빡임 버그 수정**: 시야 경계에서 적이 프레임마다 나타났다 사라지던 것을 0.22초 유지로 완화. 산탄·기관총처럼 한 틱에 여러 발 맞을 때 튐 효과가 겹쳐 캐릭터가 크게 일그러지던 것도 수정(0.1초에 한 번, 크기 제한).
- 캐릭터 위는 체력 바만, **기력 바는 캐릭터 오른쪽 세로**. 왼쪽 아래 상태창을 올려 아래 조작 안내와 겹치지 않게 하고, 안내는 화면 폭 전체 띠로.

### 배경음·효과음
- 배경음을 **132 BPM 단조 추격 루프**로 교체(킥·스네어·하이햇 + 16분 베이스 + 아르페지오 + 리드 모티프 + 8마디마다 상승음). 교전이 벌어지면 레이어가 두꺼워진다. 전부 절차 생성이라 저작권 문제가 없다.
- 기관총·후라이팬(금속 울림)·총알 막기·모래주머니 파괴 효과음 추가.

### 성능
- 시뮬레이션 벤치마크 테스트 추가(`tests/perf.test.ts`). 4인·4배 맵·봇 4명이 **틱당 0.024ms**(60Hz 예산 16.6ms의 0.15%), 봇 없는 실제 대전은 틱당 0.001ms. 방이 여러 개여도 서로 영향이 없다(각 방이 독립 P2P 메시).

## 2026-09-04 (3차)

### 멤버 12명 완성 · 대시 구르기 + 무적
- **새 멤버 7명** (`core/characters.ts`, 외형은 참고 사진을 받으면 다듬는다): 우원덕(우원박, 권총, "연기" 대시 후 0.2초 무적) · 기열덕(기열킹, SMG, "뇌절" 연속 명중 +8%씩 최대 +40%, 빗나가면 초기화) · 풍월덕(풍월량, 소총, "바람" 대시 쿨 절반·체력 85) · 옥냥덕(옥냥이, 저격, "냉정" 정조준 이동 감속 없음) · 통닭덕(통닭천사, 산탄총, "치킨" 킬 시 50 회복) · **주우재덕**(주우재, 소총, "런웨이" 대시 거리 +50%; 갓보기덕은 사용자 요청으로 제외) · 승우덕(승우아빠, 권총, "요리" 리스폰 2초·스폰 보호 3초). 패시브는 전부 sim 에 구현(`streak`, `hitSomeone`).
- **대시 = 구르기**: 대시 방향으로 몸통이 한 바퀴 구르며 살짝 뜬다(렌더). **대시 중에는 맞지 않는다**(sim, 결정론).
- 로비 캐릭터 6열 2줄, 선택 창 6열(숫자 키는 1~9까지, 나머지는 클릭), 프리뷰 시트는 7명 이상이면 두 줄.
- 테스트 20개(12명 조합 결정론·종료, 대시 무적).

### 로비 초상화 달걀형 통일 · HUD 남색 톤 · 설계서 초안 표시
- `render/character.ts drawPortrait`: 3D 와 같은 달걀형 일체 몸통(머리 원이 달걀 위, 아래로 옷·V넥·명찰·넥타이·바지·작은 다리). 로비 카드·캐릭터 선택 창 모두 적용.
- `render/hud.ts`: 패널·글자 색을 로비 팔레트(#0d1117 계열, #e6edf3, #a9b4c0, 금색 테두리)로.
- `docs/DESIGN.md`: 0~5장이 v0.1 초안 원문임을 상단에 명시.

## 2026-09-04 (2차)

### 비용 원칙 확정 (문서)
- 사용자 결정: **카드 등록이 필요한 서비스는 무료 티어라도 전부 배제**. TURN(metered.ca·Cloudflare Realtime) 제외, NAT 실패는 핫스팟 안내가 최종. 허용: GitHub Pages, Google Fonts, 공개 Nostr 릴레이, 무료 STUN, Cloudflare Workers 무료 플랜(카드 없음, 한도 초과 시 차단·시그널링 용도). `docs/PREP.md` C, `docs/DESIGN.md`, `HANDOVER.md`, `README.md` 에 반영.

### 로비 닉네임
- 캐릭터 구분선 오른쪽에 닉네임 입력(선택, 8자, localStorage 기억). 방 슬롯은 닉네임을 크게, 캐릭터 이름을 작게 보여 주고, `hello`/`room`/`start` 에 `name` 이 실려 HUD 이름표·점수판·킬 배너에 쓰인다. 비어 있으면 캐릭터 이름(중복 시 번호). 봇은 캐릭터 이름.
- 방 목록 핑 표시는 연결 전에는 RTT 를 알 수 없어 보류(연결 후 방 화면에 ms 표시는 있음). 게임 종료 후 로비로 돌아오면 로비 방에 자동 재접속된다(기존 동작).

### 효과음·배경음 (파일 없이 Web Audio 절차 생성)
- `src/audio/sfx.ts`: 무기별 총성(권총·SMG·소총·산탄총·저격), 피격/헤드샷, 사망, 리스폰, 대시, 재장전, 벽 튐(40ms 제한), 카운트다운 틱, 시작 화음, 종료 화음, 캐릭터 선택 블립. 듣는 위치(나, 관전은 산 사람 중심) 기준 거리 감쇠·좌우 패닝, 컴프레서.
- 배경음: Am–F–C–G 96 BPM 루프(베이스 + 아르페지오 + 하이햇)를 룩어헤드 스케줄러로 생성. 게임·프리뷰에서 재생.
- 음소거: 게임 오른쪽 위 "소리 켜짐/꺼짐" 버튼, `N` 키(프리뷰도). localStorage 에 기억. 브라우저 정책상 첫 클릭·키 입력 후부터 소리가 난다.
- 나중에 실제 음원을 쓰려면 `public/sfx/` 에 넣고 `sfx.ts` 의 해당 메서드를 파일 재생으로 바꾸면 된다.

### 매직덕 사진 반영 · 걷기/사망 연출 · 시야 벽 옆면 · 단군덕 패시브 · 봇 튜닝
- **매직덕**: 실제 사진 기준으로 교체 — 검은 짧은 머리 + 옆으로 넘긴 앞머리(`hair: 'fringe'`, 3D 는 이마 한쪽 캡), 얇은 검은 사각 안경, 이 드러나는 웃음, 턱 수염 자국, 흰 가운 대신 **남색 V넥 스크럽 + 검은 속옷 + 노란 명찰**(`neck: 'v'`, `undershirt`, `badge`). 몸통 텍스처에 V넥·속옷·명찰 그리기.
- 걷기: 달걀 몸이 좌우로 뒤뚱 + 통통, 팔 살짝 흔들림. 사망: 뒤로 벌렁 넘어지며 튄다.
- 시야: 벽·상자 **옆면**도 시야 밖이면 어둡게(바닥과 맞닿은 면마다 세로 사각형, 밝기는 그 앞 바닥 타일 기준).
- 단군덕 패시브 "중계" 재정의: **시야 밖 상대가 총을 쏘면 그 위치가 1.2초 표시**(화면 안이면 퍼지는 링 + "총성", 밖이면 가장자리 화살표). 문구 수정.
- 봇: 마지막으로 본 위치 기억 360 → 600틱. 보통 난이도는 넓은 맵에서 영영 못 만나지 않도록 상대 근처(±10타일)를 배회 목표로 삼는다.

### 덕코프식 일체형 캐릭터 · 샷건 상향
- **캐릭터를 다시 만듦**: 머리·몸이 하나인 **달걀형 몸통**(위가 살짝 좁음)에 얼굴·옷·넥타이·바지가 한 장 텍스처(`faceTexture.ts bodyTexture`, 구 등장방형 UV, 정면 u=0.25)로 그려진다. 머리카락·모자·귀·마이크는 달걀 표면을 따라가는 캡 메쉬(`eggify`), 팔은 옷깃 높이 옆구리의 짧은 캡슐이 총을 앞에 들고, 다리는 바닥의 작은 캡슐. 몸 크기 = 대두·덩치 가중(`eggRadius`). 침착덕 모자에 "침착" 라벨 텍스처. 이름표 높이는 `rig.height`.
- **샷건**: 탄당 15 → 22 (7발, 최대 154), 발사 간격 45 → 42틱, 퍼짐 12°/8° → 10°/6°, 거리 감쇠 완화(170px 까지 100%, 470px 에서 35%).

## 2026-09-04

### 노트북 환경 준비 · 새 방향 (둥근 캐릭터 · 최대 4인 · 맵 확장 · 시야 차단 · 카메라 45°)
- 노트북(BOOK-K87ADP9N6K)에 Node.js 24.19.0 · gh CLI 2.100.0 을 winget 으로 설치, `npm ci`. 수동 항목은 `docs/노트북-설정.md`.
- 사용자 새 요청: ① 캐릭터를 둥글고 귀엽게 ② 방 정원 최대 4명 ③ 인원에 따라 맵 최대 4배 ④ 덕코프처럼 벽 뒤·시야 밖 적은 안 보이게 ⑤ 맵이 대각선으로 보이게(카메라 요 45°). 4인 방식은 개인전(FFA) 먼저, 2v2 팀전도.

### 문서 갱신
- `HANDOVER.md` 를 v0.4(2~4인·시야·45°·둥근 캐릭터·교체) 기준으로 다시 씀. `docs/DESIGN.md` v0.4 변경 요약, `docs/공지글-모음.md` 문안 갱신(5인·시야·4인·Tab 교체·연사), `docs/PREP.md` 노트북 환경(E-2)·F-1 완료 체크, `README.md`.

### 로비·대기실 UI 다듬기 (개리그 매니저 팀 선택 화면 계열)
- `ui/style.css` 전면 교체: 깊은 남색 바탕(#0d1117 계열) + 상단 금빛·푸른빛 무대 조명, 그라데이션 제목, 시즌 필, 특징 칩, 금선 구분 머리말, 캐릭터 카드(테마색 띠·선택 시 금테 글로우·호버 리프트), 유리질 모드 패널, 초록 유리 진행 버튼(지나가는 빛) · 노란 보조 버튼 · 테두리 버튼.
- 대기실: 등장 애니메이션, 상단 금→파랑 띠, 슬롯 카드(내 슬롯 금테, 준비 시 초록 글로우, 팀 A/B 배지, 빈 슬롯 점선), 준비 = 초록 유리 버튼, 팀 바꾸기.
- `ui/lobby.ts`: 머리글(시즌 필·특징 칩·설명), 카드 구조(세로), 주요 버튼 `btn main`.

### 둥글둥글한 캐릭터
- `render3d/character3d.ts` 전면 교체: 머리 = 구(가로 1 : 세로 1.04), **얼굴은 구 표면에 150° 로 감기는 이목구비 텍스처**(피부·귀·머리카락·모자는 3D 메쉬), 몸 = 콩 모양 타원체, 팔·다리·총열 = 캡슐, 손·신발 = 눌린 구. 머리카락은 구 캡(짧은 머리·바가지·삭발), 철면덕 삐죽 원뿔 + 옆·뒤로만 감기는 회색 띠 + 별, 침착덕 모자 돔·챙·라벨, 단군덕 마이크.
- `render/character.ts` `featuresOnly` 옵션(이목구비만), `render3d/faceTexture.ts` 512px 투명 텍스처.
- 확인: `preview.html?sheet=1` (5인), `&focus=cheolmyeon` (근접).

### 시야 제한 (덕코프식 전장의 안개)
- `render3d/vision.ts`: 나(팀전은 우리 팀 전원)의 위치에서 200개 레이를 벽까지 쏘아 만든 시야 다각형 ∩ 반경 13타일. 그 밖의 바닥·벽 윗면·상자 윗면을 어두운 덮개(캔버스 마스크 텍스처)로 가리고, **적은 시야 안(벽에 안 가리고 반경 안)일 때만 그린다**. 탄·이펙트는 그대로 보인다. 죽어 있는 동안은 마지막 자리에서 본다. 관전(프리뷰)은 전부 보임.
- 렌더 전용이라 sim·결정론에 영향 없음(치트 방지는 원래 불가).

### 리스폰 중 캐릭터 교체(Tab) · 상대 정보 숨김 HUD · 모든 무기 연사
- **캐릭터 교체**: 죽어서 대기 중이거나 리스폰 후 3초 안에 `Tab` → 소환이 풀리고(맞지 않음) 선택 창. `1~5` 또는 클릭으로 고르면 남은 대기 시간이 끝난 뒤 상대에게서 먼 곳에 새 캐릭터로 리스폰. `Esc` 는 그대로. 선택은 입력 패킷(6바이트째 `char`)에 실려 가므로 대전에서도 결정론 유지. 봇은 바꾸지 않는다. 카운트다운 동안은 `aliveTicks` 를 세지 않아 시작 직후 3초도 교체 가능.
- **HUD**: 상대의 체력·기력·탄약은 보이지 않는다. 상대는 맞은 직후 2.5초만 머리 위에 체력 바. 내 캐릭터 위에는 체력·기력(대시) 바 상시. 왼쪽 아래 내 카드는 그대로, 오른쪽 아래 목록은 이름·킬·상태만(관전·아군은 체력 표시). 2인 대전도 같은 배치.
- **무기**: 권총·산탄총·저격총도 꾹 누르면 발사 간격마다 연사, 탄이 떨어지면 자동 재장전 (매번 클릭할 필요 없음).
- 캐릭터가 바뀌면 그 사람 리그만 다시 만든다. 테스트 18개(교체 흐름·3초 경과 무시 포함).

### 인원별 맵 확장 · 카메라 요 45° (덕코프식 대각선 맵)
- `core/maps.ts`: `expandRows(rows, scale)` — 2명 1배, 3명 가로 2배(거울), 4명 가로·세로 4배. 테두리 벽 한 줄을 빼고 거울로 붙여 이음새가 열린 통로가 된다. 스폰도 같이 늘어난다. `scaleForPlayers(n)`. `buildMap(id, scale)`.
- 세션·로비(start 메시지 `scale`)·프리뷰(`?scale=`) 에 배율 전달. HUD 왼쪽 위에 `×4` 표시.
- `render3d/camera.ts`: 피치 55° + **요 45° 고정**. 맵 축이 화면 대각선이라 위/아래로 보이는 거리 차이가 줄어든다. WASD 는 화면 기준(W = 화면 위)이며 `moveDirFromScreen()` 이 월드 8방향으로 정확히 대응시킨다(요가 45° 배수라 손실 없음). 봇·조준·탄은 월드 좌표라 영향 없음.
- 테스트 16개: 맵 확장 대칭·테두리·스폰 4배, 4배 맵 4인 봇 경기 종료, 카메라 기준 8방향 일대일 대응.

### 방 정원 4명 · N인 락스텝 (같은 PC 탭 3개로 검증)
- `net/room.ts`: 방은 풀 메시. 호스트가 멤버 순서·준비·팀을 `room` 메시지로 방송(정본), 정원 초과는 `full`. `start` 에 플레이어 배열(순서 = 인덱스, 호스트 0). 피어별 RTT. APP_ID 를 v2 로 올려 구버전과 섞이지 않게. **Trystero 는 훅마다 리스너 하나만 유지**하므로 안에서 한 번만 등록하고 콜백 배열로 나눠 준다(이전 코드는 로비·세션·핑 핸들러가 서로 덮어쓰고 있었다).
- `net/lockstep.ts`: 플레이어별 입력 버퍼, `hasAll`/`get` 은 N인, `drop()` 으로 이탈자 입력은 빈 입력 취급.
- `game/session.ts`: 호스트가 해시 비교 후 어긋난 게스트에게만 리싱크. 이탈은 호스트가 앞선 틱(`drop`)을 정해 모두가 같은 틱에 `dropPlayer`. 호스트가 나가면 방 종료, 남은 인원 2명 미만이면 종료 안내. 리매치 때 이미 나간 사람은 빠진 채 시작.
- `ui/lobby.ts`: 방 화면 슬롯 4칸(호스트·나·팀 배지·준비), 개인전/2v2 팀전 선택, 팀 바꾸기, 둘 이상 모두 준비 시 자동 시작. 방 목록에 인원 n/4 · 모드 표시.
- 검증: 탭 3개 → 방 만들기 → 목록에서 참가 ×2 → 캐릭터 변경·준비 → 3인 경기 시작(tick 동기 진행) → 한 명 나가기 → 양쪽 `left` 처리, 남은 둘 계속.

### core 를 2~4인 구조로 (개인전 + 팀전)
- `state.ts`: `players: PlayerState[]`(2~4), `team`(개인전은 자기 인덱스), `left`(도중 이탈), `winner` 는 팀 번호. `teamKills`, `isTeamMatch`, `isEnemy`.
- `sim.ts`: 초기 스폰은 서로 가장 먼 곳, 리스폰은 살아있는 적 모두에게서 먼 곳. 탄은 적(다른 팀)만 맞힘 → 팀전은 아군 피해 없음. 승리는 팀 킬 합계. `dropPlayer()` 로 이탈자 처리.
- `bot.ts`: 표적 선택(보이는 적 중 가장 가까운 사람, 현재 표적 우대), 표적이 바뀌면 리드샷 기억 초기화.
- 렌더러·HUD·세션·프리뷰 N인 대응: 점수판(2명은 기존, 3~4명은 이름·킬 한 줄, 팀전은 A팀:B팀), 3~4명은 내 카드 + 오른쪽 아래 상대 목록, 이름표 팀 색, 시네마틱 카메라는 산 사람 전부를 담는 범위. 같은 캐릭터가 여럿이면 "철면덕 2".
- 로비 혼자 하기: 봇 수 1~3, 개인전/2v2(봇 3, 팀 [나+봇] vs [봇+봇]). 프리뷰 `?n=4`, `?teams=1`.
- 테스트 12개: 4인 FFA 결정론·종료·스폰 간격·이탈, 2v2 아군 피해 없음·팀 킬 승리, 맵 무결성, 마당 봇 경기 종료.

## 2026-09-03

### 다른 PC 에서 이어가기 문서
- `README.md` 신설: 클론·설치·실행·커밋 작성자 설정·gh 로그인·Claude 이어가기 한 줄·명령·구조·조작·고지.
- `HANDOVER.md` 에 "다른 PC 에서 이어가기" 절차(pull 먼저, OneDrive 충돌 주의), `docs/PREP.md` 에 F-1 노트북 준비물 체크리스트.

### P2P 두 브라우저 검증 통과 · 백그라운드 탭에서도 도는 틱
- `src/game/ticker.ts`: 전용 Web Worker 가 16ms 마다 신호 → 시뮬레이션이 탭이 뒤에 있어도 진행된다(rAF 는 렌더만). 상대가 창을 잠깐 뒤로 보내도 락스텝이 멈추지 않는다.
- `lockstep.ts`: ArrayBuffer 로 도착한 입력 패킷도 처리.
- 검증(같은 PC, 탭 2개): 방 만들기 → 방 목록 표시 → 참가 → 양쪽 준비 → 자동 시작 → 호스트/게스트 틱 460/462 → 702/705 (4초, 60Hz 동기 진행). 다른 네트워크 간 NAT 검증은 `docs/PREP.md` A 항목대로 사용자가 진행.
- 디버그 훅: 게임 중 콘솔에서 `__bd.tick()`, `__bd.phase()`.

### Three.js 3D 전환 (결정 B) · 방 목록/준비 시스템 · 맵 2종 · 산탄총 거리 피해
- **3D 렌더러** `src/render3d/`: `renderer3d.ts`(원근 카메라 55° 고정, 그림자, 탄·머즐 점광·파티클·링, 이름표·HUD 오버레이), `world3d.ts`(바닥 텍스처, 벽·상자 InstancedMesh, 조명·안개), `character3d.ts`(프리미티브 치비: 다리 걷기, 총 쥔 팔이 몸과 함께 조준 회전, 머리 상자 + 얼굴 텍스처, 머리카락·모자·마이크 메쉬, 넘어지는 사망), `faceTexture.ts`(2D 캐리커처를 정면으로 그려 텍스처화).
- HUD 를 `src/render/hud.ts` 로 분리(2D 오버레이 캔버스). **2D 렌더러 `render/canvas.ts` 삭제.**
- 프리뷰·세션 모두 3D. 프리뷰 `?sheet=1` 은 3D 캐릭터 시트, `M` 키로 맵 전환.
- **맵 레지스트리** `src/core/maps.ts`: 스튜디오(실내) + 마당(야외, 상자 엄폐물). 항목 추가만으로 확장. 로비·세션·네트워크 메시지에 `map` 전달.
- **산탄총 거리 감쇠**: 140px 까지 100%, 440px 에서 28%. 탄당 피해 15×7 로 상향(근접 압도). SMG 도 약한 감쇠. `weapons.ts falloff()`.
- **로비 재설계**: 캐릭터 5칸 동일 크기, 배지·설명 제거, 맵 선택, **실시간 방 목록**(공용 로비 방에서 호스트가 방 정보 방송, 7초 미수신 시 목록에서 제거), 코드 직접 입력 폴백, 방 화면에서 **양쪽 준비 → 자동 시작**.
- `net/room.ts`: `openLobby()`(방 목록), `ready`/`start(map)` 메시지.

### 철면덕 마스코트 정밀 반영 · '주인공' 표기 제거 · 방향 재검토로 일시 정지
- 철면덕: 넓은 볼·턱살 얼굴 윤곽(전용 경로), 큰 귀(볼 높이, 앞쪽), 치켜든 굵은 눈썹, 가늘게 뜨고 옆을 흘기는 눈, 콧구멍 보이는 큰 코, 오므린 두 겹 입술, 팔자·턱살·턱 주름, 회색 옆머리 띠 위 검은 삐죽 머리 + 흰 별.
- 게임 내 '주인공' 문구 제거 → 배지는 '배도라지장'. 프리뷰 `?sheet=1&focus=<id>` 한 명 확대 모드.
- 게임 화면 오버레이가 `hidden` 상태에서도 보이며 클릭을 가로채던 CSS 버그 수정(`.overlay[hidden]{display:none}`).
- 혼자 하기 모드 브라우저 실행 확인(로비 → 카운트다운 → HUD).
- **개발 일시 정지.** 사용자 요청으로 엔진·시각 방향 재검토. `docs/DECISIONS.md` 작성(2D 강화 / Three.js 3D / Godot / Unity 비교, 결정 체크리스트 10개, 기본안).

### 캐릭터 참고 자료 반영 · 게임 본편 골격
- 철면덕: 공식 마스코트(철면수심 쇼핑몰 로고) 기준 — 빨간 얼굴, 삐죽삐죽한 검은 머리 + 정수리 흰 별, 회색 옆머리, 반쯤 감은 눈, 쭉 내민 입술, 큰 귀.
- 침착덕: 유튜브 배너/아바타 캐릭터 기준 — 노란 얼굴, 자주색 야구모자 + 흰 챙 + "침착" 라벨, 굵은 눈썹, 콧수염 + 점박이 수염, 빨간 입술, 흰 티.
- 단군덕: 첨부 사진 기준 — 검은 앞머리, 파란 원형 선글라스, 노래하는 입, 파란 물방울 재킷 + 흰 셔츠 + 나비넥타이, 마이크.
- 매직덕: 머리 1.6배, 최대 체력 150(최고). 주펄덕 유지.
- 게임 본편 골격: `src/game/session.ts`(혼자 하기/락스텝 공용 루프, 일시정지·결과·리매치), `src/game/localInput.ts`, `src/net/room.ts`(Trystero 방 코드/링크, RTT), `src/net/lockstep.ts`, `src/ui/lobby.ts`(캐릭터 선택·혼자 하기·방 만들기·방 참가). 아직 브라우저 검증 전.

### 쿼터뷰 투영 · 주인공 철면수심 · 단군 추가
- 렌더러에 기울인 카메라 투영 도입(`PITCH = 0.72`). 바닥과 벽 윗면은 y축 압축, 벽은 34px 위로 돌출하고 앞면 그라데이션·바닥 그림자를 그린다. 캐릭터는 세워서 그리고, 무기·탄·머즐은 지면 평면에 맞춰 회전한다. 3D 엔진 없이 무료.
- 카메라 줌 상향(시네마틱 1.4~2.0, 추적 1.5) — 캐릭터 얼굴이 보이도록.
- 캐릭터 `prominence` 필드 추가. 철면덕(1) > 침착덕(2) > 단군덕(3, 신규: 작은 체구·매서운 눈·캐스터 정장+빨간 넥타이·권총·최고 속도) > 매직덕(4) > 주펄덕(5). `CHARACTER_LIST` 는 이 순서.
- 프리뷰: P1 은 항상 철면덕, 상대는 우선순위 가중 무작위(첫 판은 침착덕). 타이틀 카드에 철면덕 초상. 시트에 "주인공" 배지·글자 줄바꿈.
- 테마 색 재배정: 철면 주홍, 침착 노랑, 단군 연두, 매직 하늘, 주펄 보라.
- 설계서에 "v0.2 변경 요약" 추가.

### 캐릭터를 실제 멤버 이모지풍 캐리커처로 전면 교체
- `src/render/character.ts` 신설: 머리(피부·머리카락·눈썹·눈·코·입·수염·안경·귀)와 몸(셔츠·가운·바지·신발)을 데이터(`Look`)로 그린다.
- `src/core/characters.ts` 에 `look` 필드 추가. 침착덕(짧은 검은 머리·무표정·옅은 수염·회색 티), 주펄덕(삭발·실눈·두툼한 입술·콧수염+턱수염·곤색 티), 철면덕(스포츠머리·두꺼운 눈썹·턱수염·검은 옷·큰 덩치), 매직덕(큰 머리·사각 안경·웃는 얼굴·흰 가운+파란 셔츠).
- 얼굴이 조준 방향을 따라 3/4 시점으로 돌아간다. 오리 드로잉 코드 삭제.
- 프리뷰 `?sheet=1` 캐릭터 시트 모드 추가 (정면·측면 크게).

### 프리뷰 동작 확인 · 배포 수정
- 물리: 원-타일 충돌을 모서리 법선 슬라이딩으로 교체. 봇이 벽 모서리에 걸려 영원히 멈추던 버그 수정.
- 프리뷰: 타이틀 카드 동안 sim 정지·HUD 숨김. 시네마틱 줌 1.25~1.9, 둘이 멀면 P1 추적.
- 테스트: `tests/determinism.test.ts` (해시 일치, 시드 분기, 스냅샷, 경기 종료, atan2A 근사).
- 배포: `package-lock.json` 재생성(npm ci 실패 원인: phaser 제거 후 lock 불일치), setup-node 24, `.gitattributes`(LF).
- GitHub Pages 를 API 로 활성화(`build_type=workflow`).
- 문서: `docs/PREP.md` (사용자가 직접 준비할 것 체크리스트).

### 코어 · 렌더러 · 프리뷰 골격
- Vite + TypeScript 스캐폴드, GitHub Pages 배포 워크플로(`.github/workflows/deploy.yml`).
- 결정론 시뮬레이션 `src/core/`: 고정 60Hz 틱, 이동·대시·사격·투사체·부위 피해·리스폰·목표 킬 승리.
- 봇 AI `src/core/bot.ts`: 쉬움/보통/어려움, BFS 경로 탐색, 리드샷, 회피 대시, 끼임 탈출.
- Canvas 2D 렌더러 `src/render/canvas.ts`: 3/4 시점 벽, 말랑 스프링 스케일, 탄·머즐·파티클·깃털, HUD(점수판·플레이어 카드·카운트다운·킬 배너·승리 화면), 시네마틱 카메라.
- 프리뷰 페이지 `preview.html` 골격.
- 설계 결정 변경: Phaser → Canvas 2D 자체 렌더러. 탈출 모드 제외, 혼자 하기(봇) 모드 추가.
- 문서: `HANDOVER.md`, `CHANGELOG.md` 시작.
